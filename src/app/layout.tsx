import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Spiritual Gifts Assessment | Mountain View Community Church",
  description: "Discover your spiritual gifts and find your place to serve at Mountain View Community Church.",
  keywords: ["spiritual gifts", "assessment", "church", "MVCC", "Mountain View Community Church", "serve"],
  icons: {
    icon: "/logo.png",
    apple: "/logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
